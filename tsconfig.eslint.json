{
  "//1": "// extend your base config so you don't have to redefine your compilerOptions",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx",
    "test/**/*.ts",
    "test/**/*.tsx",
    "typings/**/*.ts",
    "@types/**/*.ts",

    "// if you have a mixed JS/TS codebase, don't forget to include your JS files",
    "src/**/*.js"
  ]
}
